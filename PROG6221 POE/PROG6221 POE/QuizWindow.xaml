<Window x:Class="PROG6221_POE.QuizWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Quiz" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2D2D3D" Padding="15" CornerRadius="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock x:Name="txtTitle" Text="Cybersecurity Quiz" 
                           FontSize="24" FontWeight="Bold" 
                           Foreground="#00CED1" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtProgress" Text="Question 1 of 12" 
                           FontSize="14" Foreground="#AAA" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Score Display -->
        <Border Grid.Row="1" Background="#2D2D3D" Padding="10" CornerRadius="8" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="txtScore" Text="Score: 0" 
                           Foreground="#00CED1" FontSize="16" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" x:Name="txtStatus" Text="Ready" 
                           Foreground="#AAA" FontSize="16" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Question Display -->
        <Border Grid.Row="2" Background="#2D2D3D" Padding="20" CornerRadius="10">
            <StackPanel>
                <TextBlock x:Name="txtQuestion" Text="" Foreground="White" 
                           FontSize="18" TextWrapping="Wrap" Margin="0,0,0,20"/>

                <StackPanel x:Name="pnlOptions" Margin="0,10,0,0">
                    <!-- Options will be dynamically added here -->
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Feedback -->
        <Border Grid.Row="3" Background="#2D2D3D" Padding="10" CornerRadius="8" 
                Margin="0,10,0,10" Visibility="Collapsed" x:Name="pnlFeedback">
            <TextBlock x:Name="txtFeedback" Foreground="#AAA" FontSize="14" 
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Controls -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Start Quiz" Background="#00CED1" 
                    Foreground="#1A1A2E" FontWeight="Bold" Padding="20,10"
                    BorderThickness="0" Cursor="Hand" HorizontalAlignment="Left"
                    Click="BtnStartQuiz_Click" x:Name="btnStartQuiz"/>

            <Button Grid.Column="1" Content="Close" Background="#FF6B6B" 
                    Foreground="White" FontWeight="Bold" Padding="20,10"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnClose_Click"/>
        </Grid>
    </Grid>
</Window>